# Core
numpy>=1.22.0
scipy>=1.9.0          # bandpass filtering (butter + sosfiltfilt), interpolation
PyQt5>=5.15.0
matplotlib>=3.7.0     # constrained_layout, colorbar support

# EEG file format support
# Required for: .edf, .bdf, .mff, .dat
mne>=1.3.0

# Required for: .nsx, .ns2, .ncs, .rhd
# (falls back to built-in binary reader for .ncs if neo is unavailable)
neo>=0.12.0
